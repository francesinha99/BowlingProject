@{
}

@model IEnumerable<string>


@foreach (string t in Model)
{
    <a class="btn btn-outline-secondary @(t == ViewBag.bowlerTeam ? "btn-primary": "btn=outline-secondary")"
       asp-controller="Home"
       asp-action="Index"
       asp-route-bowlerTeam="@t">@t</a>
}




